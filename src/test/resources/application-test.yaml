spring:
  datasource:
    url: jdbc:tc:postgresql://127.0.0.1:5432/test_authentication_db
    username: test_pastebin_user
    password: test_pastebin_password
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: validate
  liquibase:
    change-log: classpath:/db/changelog.xml
