spring:
    application:
        name: authentication-service
    mail:
        host: smtp.gmail.com
        port: 587
        username: slava.gusarov.73@mail.ru #вставить свою почту
        password:
        protocol: smtp
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true

    data:
        mongodb:
            host: users_db
            port: 27018
            database: users_db
            #username: root
            #password: root
            #auto-index-creation: true
        redis:
            host: localhost
            port: 6380

jwt:
    access-token-expire-time-ms: 3600
    refresh-token-expire-time-ms: 2592000
    refresh-token-name: refresh-token

rsa:
    private-key: classpath:certs/private.pem
    public-key: classpath:certs/public.pem

application:
    unverified_user_expire_time_seconds: 3600

